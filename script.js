// 读取消息内容
const messageContent = `首先，我得承认写这玩意存粹是因为懒，毕竟打字明显比手写快。
emmm...其次，得说明，目前国内的情况很糟糕，虽然国外也没好到哪儿去，但其实比国内好得多（如果呆的下来的话）。城市的情况也不算好，今年我能知道的大型企业财政都属于炸裂级别，国资委这两年很难过。
不过最大的问题自始至终只有一个，那就是延迟3-5年退休导致的进门难问题，这事的影响太大了，造成了三个问题：
1.老的不退年轻人进不去
2.年轻人没钱群体消费力直接下降——总不能指望老的去蹦迪去high去浪
3.房价进一步烂掉了
我记得我之前用开玩笑的口吻和你说把你家房子卖了再买铁定能赚，现在，嗯，你那边三万都有人卖。我家的情况也没差，老头硬着去郊区买的房子，240w买来现在110w脱手都难。好在我可以选的两套在我的据理力争之下没卖掉，这两套也如我所料跌幅不超过10%，嗯，至少不至于无家可归。
写这个的原因主要是阐述一下国内的糟糕情况，以及一定程度上预防某个人做出糟糕的决定。
房价基本上是腰斩，我表姐在文澜1300w的房子现在卖600个我都嫌弃，有些地方甚至是膝斩（p.s.纯听说），中产阶级直接被干碎了。我有一个阿姨，一辈子30年的积蓄在郊区买了套房子，嗯，她这辈子现在只能算干了15年了。这个问题相当严重，现在国内储蓄金额嘎嘎上升，家庭花费比例库库下降，传统意义上的土地-消费循环直接被国家政策一夹当场碎裂。这几年各大企业裁员很厉害，厉害到什么程度——阿里一年就把自己的12w员工裁剪到了10w。说到这里，想起来之前去过阿里的研发院，结果达摩院那帮子人看不上我（理所应当），宁愿把我的照片挂在墙上（我去不是只有死掉的人才挂在墙上嘛），也不愿意让我进去（算了反正估摸着也就客气一下），哼。
接着说吧，所以这两年学阀们的地位史诗级上升，因为新建的小门阀在总书记上台后大部分已经被干的稀碎了，剩下几个不顶用的鹌鹑缩在暗地里。但是学阀不一样，我们需要科技叙事，所以学阀能带来产出就能活着。这句话的意思是，浙大里面应该有出路，嗯，外边的也有出路，不过明显少，而且很多都是披着出路皮的死路。
国内的普遍要求是1-3年内能带来产出，所以你懂的，拿不出来，非升即走，就可以去死了。至于高学历嘛，你这种我不知道，但是我知道qs第一的水硕回来工作难找，以及你的浙大学长去了港大回来的，还有英国留学的13年爱丁堡毕业的学生，他们都是我的朋友，我得说靠自己坟头草都一丈高了。所以你得有真的实力，如果只是参与项目而不是主持工作，没有足额的达标论文，你这身体情况回来就可以准备在家办公了（p.s.指被人赶出来）。
其实我都懒得打听你到底是不是出国，不过这事无所谓，反正我懒，就做我该做的。我的观点就是，出去好啊，能别回来30-35年之前就别回来，除了迫不得已，提前回来会被头都给打烂掉的。这两年国企的竞争程度，就这么说的，没名额是日常的。所以国企这里是这样的情况：你想进来就得把自己的岗位创造出来。
我好像是出不去了，本来说要去英国留学的，曼大的水硕。但是后面好像有回旋镖砸我头上了（有点猜测，但不准，我怀疑是我自己高中时期砸过来的），但是拉倒，不妨碍我想干的事情。现在我在某大企业里干临时工（莫名其妙的），老头好像和人聊过，他的老领导建议的。
对于未来，我的感觉是30年前就是一坨狗屎，30年后机会会慢慢的出来，重塑经济格局还是要等十年起。不过在此之前，还有很大的问题，正如我四年前和你说的一样，山雨欲来风满楼。
其中最核心的问题完全没有爆发出来。如果房地产暴雷算是第一集的第一幕，那么如今的一切只能算开幕仪式，希望你真的如你所说认真听了当初我写下来的和聊的，里面有相当多有意思的观点。哦还有，可恶啊我记得我和你打赌但是你没理我，只说我错了你们的笨逼老师认为（经济疫情后起飞）来着，气死我了，我和你讲道理讲逻辑讲知识你和我说那些笨逼的教学目标，未来有机会你得请我（吃不穷你）。现在最核心的问题是地方财政暴雷带来的信用危机，这个问题自始至终都没有变过——当然还有别的问题。不过似乎这不是新年快乐里应该说的？
那就讲一些该说的...啊...什么是该说的呢，该说的就是，如果你期待未来短中时期中国经济能好起来不如相信我是超人。我们正在系统性的减少中产，因为我们13亿人孕育不出那么多岗位，很不幸，理论上而言，如果你不能出头，你家就是被消灭的穷逼。所以...努力吧，疾风知劲草。关于更后面的情况，我们的社会体制终究是需要中产的，所以等边缘中产干碎的差不多了，核心区域内会有一批世代版的中产出现，这几乎是必然的。国家需要保证中间群体的忠诚性，到了那个时间点，房价和别的利好中产的项目应该会再次大幅攀升，这是合理的猜测。放长远来说，这也是一种必然性，如果一个政府损失了中坚的力量是愚蠢的行为，没有中间的力量则是会直接完蛋，这不是猜测而是事实。日本首相高市早苗就是其中佼佼者，我一个留学生朋友告诉我他们普遍认为高市女士是一套疯狗，实际上这位女士是有非常坚挺的民意支撑的，所以，事实告诉我们，到了21世纪，国家和政权再疯其实也是有支点的。
中国最大的核心问题自始至终都是教育，体制和理念支撑（因为我们毫无信仰可言），这仨问题围绕了近20年几乎所有的问题——当然，纯粹是我自己归纳的浅薄的看法。马上就要到第一幕最血腥的时候了，差不多就是过两年，等这批老的退了没能力了，接下来就一定会有一个结果的，好的情况是局部的震荡，坏的结局...坦白来说，就是完蛋。
我很有兴趣分享一下目前国内企业的部分底层逻辑，这可能对你有些帮助。首先，第一个问题是很多高学历的也并非真正的人才，我们得承认，教育是失败的底层逻辑，次品率太高是企业根本赌不起的重要的因素。教育体系的不可持续性，导致现在的问题是一旦5年内我们的经济炸裂，地方无力负担培养下一代的能力，要知道我们虽然反对补课，但是实际上，中国学生的能力都是卷出来的，这需要大量的民间投入，不论时间还是金钱抑或人性——换句话说就是为啥我们的生育率嘎嘎降低，因为现在我们就负担不起了。没人，我们这种杀出千万人重围的人才培养体系就会变得很幽默，至于地方创新，没人才创新个皮皮虾。目前社会就是在燃烧社会信心和社会凝聚力来让社会保持稳定。
结果就是企业必须要求你尽可能的带着利润直接进来，而不是一个随时会走得人，所以高薪的本质就是高利润的部分必要前提条件。至于低薪，你的教育程度就没有任何优势，反而是劣势，因为对于企业而言这是不必要的风险，低学历反而更受青睐。我和阿里的副总裁聊过天，具体的有一些东西你不方便知道，但是我可以告诉你两件事，首先，区分水硕和正经海外硕士海外博士的，只有能力区别，论文很次，其次，如果你在蒙特利尔，那应该含金量理论上只能在本赛道有用——除非你有杰出贡献。
嗯，顺便和你分享一下这两年空出来的时间我又学了好多，比如程序性过程的必要性、时政影响的深远性和滞后性，普通民众理解能力的超前性和行动的不作为可能。还有对于政治改革的局限性的理解，国家政策的针对性的负面影响，中世纪欧洲贵族的发展情况（最近在看的就是美第其家族史）emmm...这些是我看的书的一半不到，这两年我的时间并不多，要实习考雅思考研——就结果而言，都没考上就结束了qwq
最后祝新年快乐（虽然还有好多天），万事如意。注意锻炼身体,以及虽然眼睛不好但也别真瞎了(想不到更和平的口吻了)以及来年和后续的年份(鬼知道明年我会不会写,这个程序可以用好多年哈哈哈哈哈)事业有成,学业顺利。`;

// 农历春节日期表（公历日期，月份从0开始，所以1表示2月）
const lunarNewYearDates = {
    2024: new Date(2024, 1, 10),  // 2024年2月10日（甲辰年）
    2025: new Date(2025, 0, 29),  // 2025年1月29日（乙巳年）
    2026: new Date(2026, 1, 17),  // 2026年2月17日（丙午年）
    2027: new Date(2027, 1, 6),   // 2027年2月6日（丁未年）
    2028: new Date(2028, 0, 26),  // 2028年1月26日（戊申年）
    2029: new Date(2029, 1, 13),   // 2029年2月13日（己酉年）
    2030: new Date(2030, 1, 3),   // 2030年2月3日（庚戌年）
    2031: new Date(2031, 0, 23),  // 2031年1月23日（辛亥年）
    2032: new Date(2032, 1, 11),   // 2032年2月11日（壬子年）
    2033: new Date(2033, 0, 31),   // 2033年1月31日（癸丑年）
    2034: new Date(2034, 1, 19),   // 2034年2月19日（甲寅年）
    2035: new Date(2035, 1, 8),    // 2035年2月8日（乙卯年）
    2036: new Date(2036, 0, 28),   // 2036年1月28日（丙辰年）
    2037: new Date(2037, 1, 15),   // 2037年2月15日（丁巳年）
    2038: new Date(2038, 1, 4)     // 2038年2月4日（戊午年）
};

// 全局变量
let calendar;
let fireworksActive = false;
let celebrationStarted = false;
let envelopeOpened = false;

// 初始化
document.addEventListener('DOMContentLoaded', function() {
    initializeCalendar();
    initializeTimeDisplay();
    initializeFireworks();
    initializeEnvelope();
    initializeTestButton();
    initializeEarlyCelebrationButton();
    checkNewYearTime();
    
    // 每秒更新时间和检查是否到了新年
    setInterval(() => {
        updateTime();
        checkNewYearTime();
    }, 1000);
});

// 初始化信封点击事件
function initializeEnvelope() {
    const envelope = document.getElementById('envelope');
    const letter = document.getElementById('letter');
    const closeBtn = document.getElementById('close-letter-btn');
    
    // 点击信封打开
    envelope.addEventListener('click', function() {
        if (!envelopeOpened) {
            openEnvelope();
        }
    });
    
    // 关闭信件
    closeBtn.addEventListener('click', function() {
        closeLetter();
    });
    
    // 点击信件外部区域关闭
    letter.addEventListener('click', function(e) {
        if (e.target === letter) {
            closeLetter();
        }
    });
    
    // 初始化信件内容
    displayLetterContent();
}

// 打开信封
function openEnvelope() {
    const envelope = document.getElementById('envelope');
    const letter = document.getElementById('letter');
    
    envelope.classList.add('opened');
    envelopeOpened = true;
    
    // 延迟显示信件内容
    setTimeout(() => {
        letter.classList.add('show');
    }, 400);
}

// 关闭信件
function closeLetter() {
    const letter = document.getElementById('letter');
    letter.classList.remove('show');
}

// 显示信件内容
function displayLetterContent() {
    const letterBody = document.getElementById('letter-body');
    const letterDate = document.getElementById('letter-date');
    
    // 设置日期
    const now = new Date();
    const dateOptions = { 
        year: 'numeric', 
        month: 'long', 
        day: 'numeric'
    };
    letterDate.textContent = now.toLocaleDateString('zh-CN', dateOptions);
    
    // 设置信件内容
    const paragraphs = messageContent.split('\n').filter(p => p.trim());
    let html = '';
    paragraphs.forEach(para => {
        html += `<p>${para}</p>`;
    });
    
    letterBody.innerHTML = html;
}

// 初始化 FullCalendar
function initializeCalendar() {
    const calendarEl = document.getElementById('calendar');
    
    calendar = new FullCalendar.Calendar(calendarEl, {
        initialView: 'dayGridMonth',
        locale: 'zh-cn',
        headerToolbar: {
            left: 'prev,next',
            center: 'title',
            right: ''
        },
        buttonText: {
            today: '今天',
            month: '月',
            week: '周',
            day: '日'
        },
        height: 'auto',
        events: [
            {
                title: '春节（农历新年）',
                start: getLunarNewYearDate(),
                backgroundColor: '#ff6b6b',
                borderColor: '#ff6b6b'
            }
        ],
        eventClick: function(info) {
            alert('新年快乐！！！');
        }
    });
    
    calendar.render();
}

// 获取农历新年日期（春节）
function getLunarNewYearDate() {
    const now = new Date();
    const currentYear = now.getFullYear();
    
    // 查找当前年份或下一年的春节日期
    let newYearDate = lunarNewYearDates[currentYear];
    if (!newYearDate) {
        // 如果当前年份不在表中，查找下一年
        newYearDate = lunarNewYearDates[currentYear + 1];
    }
    if (!newYearDate) {
        // 如果还是找不到，使用默认值（当前年份+1的1月1日作为后备）
        return `${currentYear + 1}-01-01`;
    }
    
    // 如果今年的春节已过，使用明年的
    if (newYearDate < now) {
        newYearDate = lunarNewYearDates[currentYear + 1];
        if (!newYearDate) {
            return `${currentYear + 1}-01-01`;
        }
    }
    
    const year = newYearDate.getFullYear();
    const month = String(newYearDate.getMonth() + 1).padStart(2, '0');
    const day = String(newYearDate.getDate()).padStart(2, '0');
    return `${year}-${month}-${day}`;
}

// 获取农历新年日期对象
function getLunarNewYearDateObj() {
    const now = new Date();
    const currentYear = now.getFullYear();
    
    let newYearDate = lunarNewYearDates[currentYear];
    if (!newYearDate) {
        newYearDate = lunarNewYearDates[currentYear + 1];
    }
    if (!newYearDate) {
        // 后备：使用当前年份+1的1月1日
        return new Date(currentYear + 1, 0, 1, 0, 0, 0);
    }
    
    // 如果今年的春节已过，使用明年的
    if (newYearDate < now) {
        newYearDate = lunarNewYearDates[currentYear + 1];
        if (!newYearDate) {
            return new Date(currentYear + 1, 0, 1, 0, 0, 0);
        }
    }
    
    // 返回春节当天的0点
    return new Date(newYearDate.getFullYear(), newYearDate.getMonth(), newYearDate.getDate(), 0, 0, 0);
}

// 初始化时间显示
function initializeTimeDisplay() {
    updateTime();
}

// 更新时间显示
function updateTime() {
    const now = new Date();
    
    // 格式化日期
    const dateOptions = { 
        year: 'numeric', 
        month: 'long', 
        day: 'numeric',
        weekday: 'long'
    };
    const dateStr = now.toLocaleDateString('zh-CN', dateOptions);
    
    // 格式化时间
    const timeStr = now.toLocaleTimeString('zh-CN', { 
        hour12: false,
        hour: '2-digit',
        minute: '2-digit',
        second: '2-digit'
    });
    
    document.getElementById('current-date').textContent = dateStr;
    document.getElementById('current-time').textContent = timeStr;
}

// 检查是否到了农历新年时间（春节）
function checkNewYearTime() {
    if (celebrationStarted) return;
    
    const now = new Date();
    const newYearDate = getLunarNewYearDateObj();
    
    // 检查是否是春节当天的0点0分0秒
    const isNewYear = now.getFullYear() === newYearDate.getFullYear() &&
                      now.getMonth() === newYearDate.getMonth() &&
                      now.getDate() === newYearDate.getDate() &&
                      now.getHours() === 0 &&
                      now.getMinutes() === 0 &&
                      now.getSeconds() === 0;
    
    if (isNewYear) {
        startCelebration();
        celebrationStarted = true;
    }
    
    // 更新倒计时
    updateCountdown();
}

// 更新倒计时（农历新年）
function updateCountdown() {
    const now = new Date();
    const nextNewYear = getLunarNewYearDateObj();
    const diff = nextNewYear - now;
    
    if (diff <= 0) {
        document.getElementById('countdown').textContent = '春节已到来！';
        return;
    }
    
    const days = Math.floor(diff / (1000 * 60 * 60 * 24));
    const hours = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
    const seconds = Math.floor((diff % (1000 * 60)) / 1000);
    
    // 获取农历年份
    const lunarYear = getLunarYear(nextNewYear);
    
    document.getElementById('countdown').textContent = 
        `距离${lunarYear}年春节：${days}天 ${hours}小时 ${minutes}分 ${seconds}秒`;
}

// 获取农历年份
function getLunarYear(date) {
    const year = date.getFullYear();
    // 简单的农历年份计算（实际应该用农历库，这里用近似值）
    // 春节通常在1-2月，如果春节在1月，农历年份可能是上一年
    const month = date.getMonth();
    if (month === 0) {
        // 1月的春节，农历年份通常是上一年
        return year - 1;
    }
    return year;
}

// 开始庆祝
function startCelebration() {
    // 显示庆祝文字
    const celebrationText = document.getElementById('celebration-text');
    celebrationText.classList.remove('hidden');
    
    // 启动烟花
    startFireworks();
    
    // 自动打开信封
    if (!envelopeOpened) {
        setTimeout(() => {
            openEnvelope();
        }, 1000);
    }
    
    // 5秒后隐藏庆祝文字，但继续烟花
    setTimeout(() => {
        celebrationText.classList.add('hidden');
    }, 5000);
}

// 初始化烟花画布
function initializeFireworks() {
    const canvas = document.getElementById('fireworks-canvas');
    const ctx = canvas.getContext('2d');
    
    function resizeCanvas() {
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;
    }
    
    resizeCanvas();
    window.addEventListener('resize', resizeCanvas);
}

// 启动烟花特效
function startFireworks() {
    // 如果已经在运行，先停止
    if (fireworksActive) {
        stopFireworks();
    }
    
    fireworksActive = true;
    
    const canvas = document.getElementById('fireworks-canvas');
    const ctx = canvas.getContext('2d');
    
    // 清空画布
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    
    const fireworks = [];
    const particles = [];
    let animationId = null;
    
    class Particle {
        constructor(x, y, vx, vy, color, life) {
            this.x = x;
            this.y = y;
            this.vx = vx;
            this.vy = vy;
            this.color = color;
            this.life = life;
            this.maxLife = life;
            this.gravity = 0.1;
        }
        
        update() {
            this.x += this.vx;
            this.y += this.vy;
            this.vy += this.gravity;
            this.life--;
        }
        
        draw() {
            const alpha = this.life / this.maxLife;
            ctx.save();
            ctx.globalAlpha = alpha;
            ctx.fillStyle = this.color;
            ctx.beginPath();
            ctx.arc(this.x, this.y, 2, 0, Math.PI * 2);
            ctx.fill();
            ctx.restore();
        }
        
        isDead() {
            return this.life <= 0;
        }
    }
    
    class Firework {
        constructor(x, y, targetY) {
            this.x = x;
            this.y = y;
            this.targetY = targetY;
            this.vy = -8;
            this.exploded = false;
            this.color = `hsl(${Math.random() * 360}, 100%, 50%)`;
        }
        
        update() {
            if (!this.exploded) {
                this.y += this.vy;
                if (this.y <= this.targetY) {
                    this.explode();
                }
            }
        }
        
        explode() {
            this.exploded = true;
            const particleCount = 50;
            for (let i = 0; i < particleCount; i++) {
                const angle = (Math.PI * 2 * i) / particleCount;
                const speed = 2 + Math.random() * 3;
                const vx = Math.cos(angle) * speed;
                const vy = Math.sin(angle) * speed;
                particles.push(new Particle(this.x, this.y, vx, vy, this.color, 60));
            }
        }
        
        draw() {
            if (!this.exploded) {
                ctx.fillStyle = this.color;
                ctx.beginPath();
                ctx.arc(this.x, this.y, 3, 0, Math.PI * 2);
                ctx.fill();
            }
        }
    }
    
    function animate() {
        if (!fireworksActive) {
            // 清理所有粒子
            fireworks.length = 0;
            particles.length = 0;
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            return;
        }
        
        // 使用透明背景来创建拖尾效果
        ctx.fillStyle = 'rgba(0, 0, 0, 0.1)';
        ctx.fillRect(0, 0, canvas.width, canvas.height);
        
        // 随机发射新烟花
        if (Math.random() < 0.05) {
            const x = Math.random() * canvas.width;
            const targetY = 100 + Math.random() * (canvas.height * 0.4);
            fireworks.push(new Firework(x, canvas.height, targetY));
        }
        
        // 更新和绘制烟花
        for (let i = fireworks.length - 1; i >= 0; i--) {
            fireworks[i].update();
            fireworks[i].draw();
            if (fireworks[i].exploded) {
                fireworks.splice(i, 1);
            }
        }
        
        // 更新和绘制粒子
        for (let i = particles.length - 1; i >= 0; i--) {
            particles[i].update();
            particles[i].draw();
            if (particles[i].isDead()) {
                particles.splice(i, 1);
            }
        }
        
        // 继续动画
        animationId = requestAnimationFrame(animate);
    }
    
    // 开始动画
    animationId = requestAnimationFrame(animate);
    
    // 30秒后停止烟花
    const stopTimeout = setTimeout(() => {
        fireworksActive = false;
        if (animationId) {
            cancelAnimationFrame(animationId);
        }
        // 清理画布和所有粒子
        fireworks.length = 0;
        particles.length = 0;
        ctx.clearRect(0, 0, canvas.width, canvas.height);
    }, 30000);
    
    // 保存停止函数，以便外部调用
    window.currentFireworkStop = () => {
        clearTimeout(stopTimeout);
        fireworksActive = false;
        if (animationId) {
            cancelAnimationFrame(animationId);
        }
        fireworks.length = 0;
        particles.length = 0;
        ctx.clearRect(0, 0, canvas.width, canvas.height);
    };
}

// 停止烟花
function stopFireworks() {
    fireworksActive = false;
    const canvas = document.getElementById('fireworks-canvas');
    const ctx = canvas.getContext('2d');
    
    // 如果有停止函数，调用它
    if (window.currentFireworkStop) {
        window.currentFireworkStop();
        window.currentFireworkStop = null;
    } else {
        // 否则直接清理
        ctx.clearRect(0, 0, canvas.width, canvas.height);
    }
}

// 初始化测试按钮
function initializeTestButton() {
    const testBtn = document.getElementById('test-celebration-btn');
    testBtn.addEventListener('click', () => {
        if (!celebrationStarted) {
            startCelebration();
            celebrationStarted = true;
            testBtn.textContent = '🎉 庆祝中...';
            testBtn.disabled = true;
        }
    });
}

// 初始化提前过年按钮
function initializeEarlyCelebrationButton() {
    const earlyBtn = document.getElementById('early-celebration-btn');
    earlyBtn.addEventListener('click', () => {
        if (!celebrationStarted) {
            // 触发庆祝效果
            startCelebration();
            celebrationStarted = true;
            
            // 更新按钮状态
            earlyBtn.textContent = '🎆 庆祝中...';
            earlyBtn.disabled = true;
            
            // 同时更新测试按钮状态
            const testBtn = document.getElementById('test-celebration-btn');
            if (testBtn) {
                testBtn.textContent = '🎉 庆祝中...';
                testBtn.disabled = true;
            }
        }
    });
}
